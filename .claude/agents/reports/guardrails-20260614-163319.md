# Guardrails Review — 2026-06-14

**Commit:** `fcea111e11dd748dcb705aa9b67cb9ec314a212c`
**Build timestamp:** 2026-06-14T16:02:38Z
**Report generated:** 2026-06-14T16:33:19Z
**Changed files reviewed:** 2

---

## Summary

| Dimension | Status | Open Issues |
|-----------|--------|-------------|
| DDD Compliance | PASS | 0 violations (in changed files) |
| Security | CRITICAL | 9 (3C / 1H / 5M / 0L) — pre-existing, unaffected by this commit |
| **Overall** | **FAIL** | |

> Note: Both changed files pass all applicable DDD rules. The FAIL is driven by 3 pre-existing Critical security findings that remain OPEN from the prior scan at commit `a54d1ca`. These were not introduced by this commit but must be resolved before any deployment.

---

## Critical Actions Required

> Items below must be addressed before any deployment.

1. [S001] Hardcoded database password in connection string — `src/Presentation/appsettings[dot]json:10`
2. [S002] Placeholder JWT Authority value — `src/Presentation/appsettings[dot]json:13`
3. [S003] All CatalogProduct mutation endpoints lack RequireAuthorization — `src/Presentation/Endpoints/CatalogProductEndpoints.cs:23-51`
4. [S004] Entity POST mutation endpoint lacks RequireAuthorization — `src/Presentation/Endpoints/EntityEndpoints.cs:19-23`

---

## DDD Compliance Findings

**Scanner:** ddd-reviewer
**Files reviewed:** 2
**Method:** Full rule-by-rule analysis against Domain (D1-D9) and Infrastructure.Data (ID1-ID5) rule sets.

### File: `JL.Commerce.Tecnology.Service/src/Domain/Aggregates/Entity/Entity.cs`

**Layer:** Domain

| Rule | Description | Result |
|------|-------------|--------|
| D1 | Domain.csproj has zero PackageReference items | PASS |
| D2 | EntityId is `sealed record EntityId(Guid Value)` with `New()` method; no raw Guid as ID | PASS |
| D3 | Entity inherits `AggregateRoot<EntityId>` | PASS |
| D4 | Has `private Entity() {}` parameterless constructor for EF Core | PASS |
| D5 | All properties use `private set` (Name, Description, CreatedAt) | PASS |
| D6 | Public construction only via `public static Entity Create(...)` factory method | PASS |
| D7 | `Create()` calls `AddDomainEvent(new EntityCreatedEvent(entity.Id))` before returning | PASS |
| D8 | `EntityCreatedEvent` is `sealed record` implementing `IDomainEvent`, carries `EntityId` (strongly-typed, not raw Guid) | PASS |
| D9 | `AddDomainEvent()` only called within the aggregate | PASS |

**Verdict: PASS — No violations.**

### File: `JL.Commerce.Tecnology.Service/src/Infrastructure.Data/Configurations/EntityConfiguration.cs`

**Layer:** Infrastructure.Data (Configuration)

| Rule | Description | Result |
|------|-------------|--------|
| ID1 | `EntityConfiguration` implements `IEntityTypeConfiguration<Entity>` | PASS |
| ID2 | `.Ignore(e => e.DomainEvents)` is explicitly present (line 30) | PASS |
| ID3 | Strongly-typed ID uses `.HasConversion(id => id.Value, value => new EntityId(value))` (line 16) | PASS |

**Verdict: PASS — No violations.**

### Pre-existing Open DDD Violations (not in changed files)

The following violations were recorded in prior reviews and remain unresolved:

| ID | Rule | File | Description |
|----|------|------|-------------|
| V001 | D1 | `src/Domain/Class1.cs` | Leftover scaffold placeholder `Class1` in Domain project; must be removed. |
| V002 | D1 | `src/Application/Class1.cs` | Leftover scaffold placeholder `Class1` in Application project; must be removed. |
| V003 | D1 | `src/Infrastructure.Data/Class1.cs` | Leftover scaffold placeholder `Class1` in Infrastructure.Data project; must be removed. |
| V006 | P1 | `src/Presentation/Endpoints/CatalogProductEndpoints.cs:124` | `UpdateCatalogProductRequest` record declared inside endpoint file; must be moved to a dedicated DTO file. |

**Total open DDD violations: 4 (none in changed files)**

---

## Security Findings

**Scanner:** Static fallback (Semgrep 1.166.0 installed; cloud rules unavailable without login)
**Previous scan SHA:** a54d1ca0ab545aa6b1e74e9a0c68467feb6551da
**Current SHA:** fcea111e11dd748dcb705aa9b67cb9ec314a212c
**Delta:** Changed files (Entity.cs, EntityConfiguration.cs) are Domain and Infrastructure.Data layer files with no security-sensitive surface (no credentials, no SQL, no HTTP handlers, no exception exposure). All 9 prior findings remain OPEN and are confirmed unaffected by this commit.

### Critical Findings

| # | Title | Severity | File | Line | Description | Recommended Fix |
|---|-------|----------|------|------|-------------|-----------------|
| S001 | Hardcoded database password | Critical | `src/Presentation/appsettings[dot]json` | 10 | `ConnectionStrings.Database` contains `Password=postgres` — a literal credential in source control. | Move to environment variable or .NET User Secrets. Inject via env var `ConnectionStrings__Database` at runtime. |
| S002 | Placeholder JWT Authority | Critical | `src/Presentation/appsettings[dot]json` | 13 | `Jwt.Authority` contains a placeholder value, disabling token validation. | Replace with real identity provider URL. Set via env var `Jwt__Authority` and validate with `TokenValidationParameters.ValidateIssuer = true`. |
| S003 | Unauthenticated mutation endpoints (CatalogProduct) | Critical | `src/Presentation/Endpoints/CatalogProductEndpoints.cs` | 23-51 | `MapPost`, `MapPut`, `MapDelete` lack `.RequireAuthorization()`. | Chain `.RequireAuthorization()` on each mutation route or apply at route group level via `group.RequireAuthorization()`. |
| S004 | Unauthenticated mutation endpoint (Entity) | Critical | `src/Presentation/Endpoints/EntityEndpoints.cs` | 19-23 | `MapPost` for entity creation lacks `.RequireAuthorization()`. | Chain `.RequireAuthorization()` on the POST route. |

### High Findings

| # | Title | Severity | File | Line | Description | Recommended Fix |
|---|-------|----------|------|------|-------------|-----------------|
| S005 | Exception message leaked in HTTP response | High | `src/Presentation/Endpoints/CatalogProductEndpoints.cs` | 101-119 | `catch` block includes `ex.Message` in the returned HTTP body, exposing internal error details. | Replace with a generic message: `Results.Problem("An error occurred.", statusCode: 500)`. Register `UseExceptionHandler` for centralized handling. |

### Medium Findings

| # | Title | Severity | File | Line | Description | Recommended Fix |
|---|-------|----------|------|------|-------------|-----------------|
| S006 | Missing rate limiter | Medium | `src/Presentation/Program.cs` | — | `AddRateLimiter` / `UseRateLimiter` are absent; no request throttling. | Add `builder.Services.AddRateLimiter(...)` and `app.UseRateLimiter()` with a fixed window policy. |
| S007 | AllowedHosts wildcard | Medium | `src/Presentation/appsettings[dot]json` | 8 | `"AllowedHosts": "*"` accepts any Host header, enabling Host header injection. | Set to explicit hostname(s): `"AllowedHosts": "api.yourdomain.com"`. |
| S008 | Missing UseExceptionHandler | Medium | `src/Presentation/Program.cs` | — | No global exception handler middleware registered; unhandled exceptions may return 500s with stack traces. | Add `app.UseExceptionHandler("/error")` or use `app.MapProblemDetails()` with a custom `IExceptionHandler`. |
| S009 | MassTransit InMemory transport | Medium | `src/Presentation/Program.cs` | 70 | `UsingInMemory` is not production-safe; messages are lost on restart. | Replace with `UsingRabbitMq(...)` for any non-local environment; gate with an environment check. |

### Low / Info Findings

_(none)_

### Summary

| Severity | Count |
|----------|-------|
| Critical | 3 |
| High | 1 |
| Medium | 5 |
| Low | 0 |
| **Total** | **9** |

---

## Cross-Cutting Issues

> Files flagged by both reviewers require coordinated fixes.

_(none — the two changed files passed DDD review and carry no security findings. No file appears in both DDD violations and security findings for this commit.)_

---

## Recommended Fix Order

1. [S001] Hardcoded database password — `src/Presentation/appsettings[dot]json:10`
2. [S002] Placeholder JWT Authority — `src/Presentation/appsettings[dot]json:13`
3. [S003] Unauthenticated CatalogProduct mutation endpoints — `src/Presentation/Endpoints/CatalogProductEndpoints.cs:23-51`
4. [S004] Unauthenticated Entity POST endpoint — `src/Presentation/Endpoints/EntityEndpoints.cs:19-23`
5. [S005] Exception message leaked in HTTP response — `src/Presentation/Endpoints/CatalogProductEndpoints.cs:101-119`
6. [V001] Remove `src/Domain/Class1.cs` placeholder
7. [V002] Remove `src/Application/Class1.cs` placeholder
8. [V003] Remove `src/Infrastructure.Data/Class1.cs` placeholder
9. [V006] Move `UpdateCatalogProductRequest` out of endpoint file — `src/Presentation/Endpoints/CatalogProductEndpoints.cs:124`
10. [S006] Add rate limiting middleware — `src/Presentation/Program.cs`
11. [S007] Restrict AllowedHosts to explicit hostname — `src/Presentation/appsettings[dot]json:8`
12. [S008] Add UseExceptionHandler middleware — `src/Presentation/Program.cs`
13. [S009] Replace InMemory MassTransit transport with RabbitMQ — `src/Presentation/Program.cs:70`

---

## Overall Assessment: FAIL

Both files changed in this commit (`Entity.cs` and `EntityConfiguration.cs`) fully comply with all applicable DDD rules — the Domain layer and Infrastructure.Data configuration for the Entity aggregate are correctly structured with no violations. However, the service carries 3 pre-existing Critical security findings that must be resolved before any deployment: (1) remove the hardcoded database password from the app settings file and inject it via environment variable, (2) replace the placeholder JWT authority with a real identity provider URL and enable issuer validation, and (3) apply `.RequireAuthorization()` to all mutation endpoints in both `CatalogProductEndpoints.cs` and `EntityEndpoints.cs`.
