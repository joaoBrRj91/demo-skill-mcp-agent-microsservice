# Guardrails Review — 2026-06-18

**Commit:** `93ab2fe128d908147431074c7de83e89b3b39fde`
**Build timestamp:** 2026-06-18T19:43:45
**Report generated:** 2026-06-18T19:43:45
**Changed files reviewed:** 1

---

## Summary

| Dimension | Status | Open Issues |
|-----------|--------|-------------|
| DDD Compliance | PASS | 0 violations |
| Security | CRITICAL | 9 (4C / 1H / 4M / 0L) |
| **Overall** | **FAIL** | |

---

## Critical Actions Required

> Items below must be addressed before any deployment.

1. [SEC-S001] Hardcoded database password in connection string — `src/Presentation/appsettings.json:10`
2. [SEC-S002] Placeholder JWT Authority value — `src/Presentation/appsettings.json:13`
3. [SEC-S003] All CatalogProduct mutation endpoints lack RequireAuthorization — `src/Presentation/Endpoints/CatalogProductEndpoints.cs:23-51`
4. [SEC-S004] Entity POST mutation endpoint lacks RequireAuthorization — `src/Presentation/Endpoints/EntityEndpoints.cs:19-23`

---

## DDD Compliance Findings

All 9 domain rules pass for `Entity.cs`. No open violations.

| Rule | Description | Result |
|------|-------------|--------|
| D1 | `Domain.csproj` has zero `PackageReference` items | PASS |
| D2 | `EntityId` is `sealed record` with `New()` factory; no raw `Guid` ID on aggregate | PASS |
| D3 | `Entity` inherits `AggregateRoot<EntityId>` | PASS |
| D4 | Private parameterless EF Core constructor `private Entity() { }` present | PASS |
| D5 | All properties (`Name`, `Description`, `CreatedAt`, `UpdatedAt`) use `private set` | PASS |
| D6 | Only public construction path is `public static Entity Create(...)` factory | PASS |
| D7 | `Create()` calls `AddDomainEvent(new EntityCreatedEvent(entity.Id))` before returning | PASS |
| D8 | `EntityCreatedEvent` is `sealed record : IDomainEvent` carrying `EntityId` (strongly-typed) | PASS |
| D9 | `AddDomainEvent` is only called from within the aggregate | PASS |

The linter cleanup (blank line removal, commit `9a8a2e9`) introduced no compliance regressions. The `UpdatedAt` property added in a prior commit correctly uses `private set` and is set only from within the `Update()` method — fully consistent with D5 and D9.

---

## Security Findings

**Scanner:** Semgrep 1.166.0 (MCP plugin) + Static fallback cross-validation
**Scope:** Full re-scan. Modified file `Entity.cs` introduced `UpdatedAt` support via `Update()` method — no new security issues found in the changed file. All prior findings re-confirmed OPEN.

### Critical Findings

| ID | Title | File | Line | Description | Recommended Fix |
|----|-------|------|------|-------------|-----------------|
| S001 | Hardcoded database password in connection string | `src/Presentation/appsettings.json` | 10 | `Password=postgres` is committed in plain text. Any developer with repo access — or anyone who reads the file from a leaked artifact — has the credential. | Remove the password from `appsettings.json`. Use `dotnet user-secrets set "ConnectionStrings:Database" "<full-string>"` for local dev. In production override via environment variable `ConnectionStrings__Database=<value>` — env vars are loaded after appsettings and win on conflict per the ASP.NET Core configuration hierarchy. |
| S002 | Placeholder JWT Authority value | `src/Presentation/appsettings.json` | 13 | `Jwt:Authority` is `https://your-identity-provider`. The JwtBearerHandler will either fail to discover OIDC metadata or, if validation is lenient, accept tokens signed by any issuer. | Replace with the real authority URL and set explicit validation parameters: `ValidateIssuer = true`, `ValidateAudience = true`, `ValidateIssuerSigningKey = true`, `ValidIssuers = [...]`. Never ship a placeholder authority. |
| S003 | All CatalogProduct mutation endpoints lack RequireAuthorization | `src/Presentation/Endpoints/CatalogProductEndpoints.cs` | 23-51 | `MapPost`, `MapPut`, and `MapDelete` are all registered without `.RequireAuthorization()`. Any unauthenticated caller can create, update, or delete catalog products. | Chain `.RequireAuthorization()` on the `MapGroup` call so all endpoints in the group inherit it. GET endpoints that should remain public can opt out with `.AllowAnonymous()`. |
| S004 | Entity POST mutation endpoint lacks RequireAuthorization | `src/Presentation/Endpoints/EntityEndpoints.cs` | 19-23 | `MapPost("/", CreateAsync)` has no authorization guard. The route group has no authorization metadata either. | Apply group-level `.RequireAuthorization()`. The GET endpoint can opt out with `.AllowAnonymous()` if needed. |

### High Findings

| ID | Title | File | Line | Description | Recommended Fix |
|----|-------|------|------|-------------|-----------------|
| S005 | Exception message leaked in HTTP 404 response body | `src/Presentation/Endpoints/CatalogProductEndpoints.cs` | 103, 119 | Both `UpdateAsync` and `DeleteAsync` catch `CatalogProductNotFoundException` and return `TypedResults.NotFound(new { ex.Message })`. The raw domain exception message is serialized into the response body. | Remove `ex.Message` from the response. Return `TypedResults.NotFound()` with no body, or return a static RFC 7807 problem details object: `TypedResults.Problem(statusCode: 404, title: "Product not found.")`. |

### Medium Findings

| ID | Title | File | Line | Description | Recommended Fix |
|----|-------|------|------|-------------|-----------------|
| S006 | Missing rate limiter (AddRateLimiter / UseRateLimiter) | `src/Presentation/Program.cs` | — | No rate-limiting middleware is registered. Public mutation endpoints are vulnerable to brute-force and resource exhaustion. | Add `builder.Services.AddRateLimiter(...)` and `app.UseRateLimiter()`. Apply to groups with `.RequireRateLimiting("api")`. |
| S007 | AllowedHosts wildcard permits any Host header | `src/Presentation/appsettings.json` | 8 | `"AllowedHosts": "*"` disables host-header filtering. Can allow host-header injection attacks used to poison password-reset links or cache entries. | Replace with an explicit list: `"AllowedHosts": "api.example.com;localhost"`. |
| S008 | Missing UseExceptionHandler middleware | `src/Presentation/Program.cs` | — | Unhandled exceptions not caught by endpoint try/catch blocks will propagate as 500 responses, potentially exposing stack traces. | Add `builder.Services.AddProblemDetails()` then `app.UseExceptionHandler(); app.UseStatusCodePages();`. |
| S009 | MassTransit using InMemory transport (not production-safe) | `src/Presentation/Program.cs` | 70 | `x.UsingInMemory(...)` runs message delivery in-process with no durability, retry, or dead-letter support. | For production, replace with `x.UsingRabbitMq(...)`. Store credentials in environment variables or a secrets manager. |

### Summary

| Severity | Count |
|----------|-------|
| Critical | 4 |
| High | 1 |
| Medium | 4 |
| Low | 0 |
| **Total** | **9** |

---

## Cross-Cutting Issues

> Files flagged by both reviewers require coordinated fixes.

_(none — `Entity.cs` passes all DDD rules and introduces no security findings; no file appears in both DDD violations and security findings)_

---

## Recommended Fix Order

1. [S001] Hardcoded database password — `src/Presentation/appsettings.json:10`
2. [S002] Placeholder JWT Authority — `src/Presentation/appsettings.json:13`
3. [S003] CatalogProduct mutation endpoints lack authorization — `src/Presentation/Endpoints/CatalogProductEndpoints.cs:23-51`
4. [S004] Entity POST endpoint lacks authorization — `src/Presentation/Endpoints/EntityEndpoints.cs:19-23`
5. [S005] Exception message leaked in 404 response (High) — `src/Presentation/Endpoints/CatalogProductEndpoints.cs:103,119`
6. [S006] Missing rate limiter (Medium) — `src/Presentation/Program.cs`
7. [S007] AllowedHosts wildcard (Medium) — `src/Presentation/appsettings.json:8`
8. [S008] Missing UseExceptionHandler (Medium) — `src/Presentation/Program.cs`
9. [S009] MassTransit in-memory transport (Medium) — `src/Presentation/Program.cs:70`

---

## Overall Assessment: FAIL

The DDD layer is fully compliant — `Entity.cs` passes all 9 domain rules with no regressions from the recent linter cleanup. However, the service has 4 open Critical security findings that block any deployment. The two most urgent actions are: (1) remove the hardcoded `Password=postgres` from `appsettings.json` and switch to user-secrets / environment variables immediately, since the credential is already in git history; (2) add `.RequireAuthorization()` to both the `CatalogProduct` and `Entity` endpoint groups before the service handles any real traffic — currently all mutation endpoints are fully unauthenticated despite JWT middleware being registered. Fixing S001–S004 as a single batch is recommended, as they are all concentrated in `appsettings.json`, `CatalogProductEndpoints.cs`, and `EntityEndpoints.cs`.
